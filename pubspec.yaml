name: clickable_regions
description: A clickable map of The Netherlands, based on an SVG image.
author: Giovanni Terlingen <me@giovanniterlingen.com>
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  built_path: ^0.1.0
  built_path_annotations: ^0.1.0

dev_dependencies:
  build_runner: ^1.7.4

dependency_overrides:
  analyzer: ^0.39.4
  build: ^1.2.2

flutter:
  uses-material-design: true
